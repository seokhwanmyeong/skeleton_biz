import { extendTheme } from "@chakra-ui/react";

const selectTheme = {
  baseStyle: {
    field: {},
    icon: {},
  },
  sizes: {},
  variants: {},
  defaultProps: {},
};

export default selectTheme;
