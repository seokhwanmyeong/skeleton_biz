import { extendTheme } from "@chakra-ui/react";

const selectTheme = extendTheme({
  components: {
    Select: {
      baseStyle: {},
      sizes: {},
      variants: {},
      defaultProps: {},
    },
  },
});

export default selectTheme;
