// Lib
import { extendTheme } from "@chakra-ui/react";

const textAreaTheme = extendTheme({
  components: {
    Textarea: {
      baseStyle: {},
      sizes: {},
      variants: {},
      defaultProps: {},
    },
  },
});

export default textAreaTheme;
