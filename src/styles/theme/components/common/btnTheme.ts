// Lib
import { defineStyleConfig } from '@chakra-ui/react'
import { mode, StyleFunctionProps, GlobalStyleProps } from '@chakra-ui/theme-tools';

const btnTheme = defineStyleConfig({
  baseStyle: {
    color: 'themeColor'
  },
  sizes: {},
  variants: {},
  defaultProps: {},
})

export default btnTheme
