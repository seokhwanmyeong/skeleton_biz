import React from "react";
import { Flex } from "@chakra-ui/react";

type Props = {};

const SementicFilter = (props: Props) => {
  return (
    <Flex
      w="100%"
      h="100%"
      alignItems="center"
      justifyContent="center"
      borderBottom="1px solid"
    >
      SementicFilter
    </Flex>
  );
};

export default SementicFilter;
